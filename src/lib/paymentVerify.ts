/**
 * 金流 Webhook 簽名驗證與冪等性工具（Vitest 共用）
 *
 * ⚠️ 此為轉發模組（re-export wrapper）。
 *    單一來源（Single Source of Truth）位於：
 *    supabase/functions/_shared/paymentVerify.ts
 *    禁止在此處複製實作。
 */
export * from '../../supabase/functions/_shared/paymentVerify';
