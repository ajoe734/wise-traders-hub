/**
 * 付款→訂閱原子性處理工具（Vitest 共用）
 *
 * ⚠️ 此為轉發模組（re-export wrapper）。
 *    單一來源（Single Source of Truth）位於：
 *    supabase/functions/_shared/paymentProcessor.ts
 *    禁止在此處複製實作。
 */
export * from '../../supabase/functions/_shared/paymentProcessor';
