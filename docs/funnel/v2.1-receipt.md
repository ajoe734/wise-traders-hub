# Funnel v2.1 Phase 0-4 Receipt

Scope: Preview only. Schema/RLS/RPC/migration/deploy/Publish = 0.

## Changed files (allowlist)
new: src/lib/complianceCopy.ts, src/lib/preserveUtm.ts,
src/components/evidence/{EvidenceCard,WeekTimelineItem,StatusChip}.tsx,
src/pages/_expert/{DeliveryCards,SampleStructureCard,FitCard,StickyPlanCta}.tsx,
src/pages/_index/WeeklyDeliveryBridge.tsx,
src/test/unit/{complianceCopy,preserveUtm,performanceOverviewPanel.state}.test.*
modified: src/pages/{ExpertProfile,Experts,Pricing,Index}.tsx, src/components/ExpertCard.tsx,
src/components/strategy/PerformanceOverviewPanel.tsx (additive onStateChange only),
src/hooks/useExpert.ts, src/types/index.ts, src/App.tsx, src/lib/analytics/events.ts, src/index.css

## Checks
- tsgo --noEmit: exit 0, 0 errors
- vitest run (full): 234 files passed / 2 skipped; 2941 passed / 8 skipped; exit 0
- bun run build: built in 28.18s, exit 0
- static guard: 0 `supabase.from('trade_records'|'expert_signals')` in changed files
- banned-term scan on new marketing copy: 0 (only the rule comment in complianceCopy.ts)

## Preview acceptance (localhost:8080, logged-out)
Viewports 390 / 380 / 560 / 1280 on /experts, /pricing, /expert/sharkgu,
/expert/master-brian, /expert/master-zhou, /, /holding-checkup.
- console errors: 0 on every page x viewport
- trade_records / expert_signals network requests: 0 on every page x viewport
- horizontal overflow: none on every page x viewport (see Phase 5 below for the
  /pricing fix; previously 448/435/665 at 390/380/560).
- /s/sharkgu?utm_source=ig&utm_campaign=aug&secret=1
  -> /expert/sharkgu?utm_source=ig&utm_campaign=aug (secret dropped)
- /expert/sharkgu checkout CTA href:
  /checkout/sharkgu/ab1d8e55-290b-43a8-8cbb-b94dcc937200?utm_source=ig&utm_campaign=aug
- sticky mobile CTA present (1), delivery cards present (1), structure sample present (1)

Screenshots: /tmp/browser/funnel/*.png ; raw report: /tmp/browser/funnel/out.json


## Phase 5 — /pricing controlled fix round (2026-08-22)

Added to allowlist (exact files only):
src/pages/_pricing/{PricingPlanCard,PricingComparisonSection,PricingFaq,PricingExampleModal}.tsx,
src/test/unit/pricing-mobile-copy.contract.test.ts (new),
src/test/pricing-copy-consistency.test.ts (canonical copy updated).
Already in allowlist: src/pages/Pricing.tsx.

### Exact overflow root (measured, not guessed)
`Pricing.tsx` mobile branch rendered both plan cards as
`DIV.absolute inset-x-0 mx-auto` inside a `relative h-[520px]` 3D carousel; the
inactive card carried `transform: translateX(60%) translateZ(-80px) rotateY(-8deg)`
with `width:75%`, so its border box extended past the viewport:
- 390px: offender rect left 235 → right 448 (documentElement.scrollWidth 448)
- 380px: left 229 → right 435 (scrollWidth 435)
- 560px: left 341 → right 665 (scrollWidth 665)
All descendant offenders (card bg layers, header, CTA, accordion) were children of
that transformed wrapper — single root cause. Fixed by deleting the carousel, NOT by
`overflow-x: hidden/clip` (contract test asserts neither string exists).

### Layout change
`<=767px`: `grid grid-cols-1` normal document flow, both cards fully readable.
`>=768px`: `md:grid-cols-2` unchanged. Removed: absolute stacking, translate/rotate/
perspective, swipe handlers, chevron controls, dot indicators, `animate-swipe-hint`,
`mobileSelectedIndex`/`hasInteracted`/`showHint` state, `useIsMobile` usage.

### Copy convergence (mentor = neutral, complianceCopy-aligned)
- plan title: 「每週交易紀錄與心法公開」 → 「當週操作復盤＋下週觀察框架」
- painPoint: removed 「利用老師的心法決定下週出手」 → 「週末才有空，想把整週的操作看懂再做功課」
- chips/features → 當週復盤 / 判斷依據 / 風險與部位條件 / 下週研究清單與觀察條件
- comparison: T+7 週記／上週紀錄／下週再出手 → 每週固定週次公開・當週操作復盤・
  下週研究清單與觀察條件；row label 出手節奏 → 使用節奏; header/caption 去 T+7
- example modal: 「已解鎖（T+7 歷史）」→「已公開週次」;「週報教學樣式」→「當週復盤樣式」;
  「本週交易紀錄樣式」→「當週操作紀錄樣式」;「第一目標價」→「第一出場條件」
- banned terms 下週出手 / T+7 / 保證 / 目標價: 0 occurrences across all 5 pricing files
- advisor 「跟單派」 brand name retained; 「跟單」 does not appear in any mentor copy

### FAQ contract fixes
- 健檢: 「只要跟單派訂閱有效就可加購」 → 「可獨立訂閱，不必綁定任何老師；依方案每月額度使用」,
  now consistent with CheckupPlansSection 「可獨立訂閱，無需綁定老師」.
- 通知: removed 「未來我們將支援 LINE 推播」 (contradicted live truth: edge function
  `line-push-signal` ships LINE push today) → app 即時訊號牆 ＋ 已綁定 LINE 官方帳號者
  同步收到推播.

### Checks (fresh runs, this round)
- `bunx tsgo --noEmit`: exit 0, 0 errors
- targeted: pricing-mobile-copy.contract (8) + pricing-copy-consistency (8) = 16 passed
- full regression `bunx vitest run`: 235 files passed / 2 skipped; 2951 passed / 8 skipped; exit 0
- `bun run build`: built in 24.21s, exit 0
- static guard: 0 `supabase.from('trade_records'|'expert_signals')` in changed files
- no `overflow-x-hidden` / `overflow-x-clip` introduced (asserted by contract test)

### Preview re-measurement (/pricing, logged-out)
| viewport | doc scrollWidth/clientWidth | body scrollWidth/clientWidth | offenders | card rects |
|---|---|---|---|---|
| 390 | 390 / 390 | 390 / 390 | 0 | (16,467,358x496) & (16,987,358x496) → single column |
| 380 | 380 / 380 | 380 / 380 | 0 | (16,467,348x496) & (16,987,348x496) → single column |
| 560 | 560 / 560 | 560 / 560 | 0 | (16,445,528x496) & (16,965,528x496) → single column |
| 1280 | 1280 / 1280 | 1280 / 1280 | 0 | (192,471) & (652,471) side-by-side → two columns |

- first CTA (`選跟單派`) reachable at every viewport (visible, y=776/776/754/780)
- carousel artifacts (`animate-swipe-hint`) absent at all viewports
- console errors: 0; HTTP 4xx/5xx: 0; trade_records/expert_signals requests: 0 (all 4 viewports)
- screenshots: /tmp/browser/pricefix/pricing_{390,380,560,1280}_{top,bottom}.png —
  bottom shots show FAQ + footer disclaimer fully visible, no sticky CTA overlay on /pricing

Publish / deploy / schema / RLS / production data: 0.
