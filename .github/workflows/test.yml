name: Vitest

on:
  push:
    branches: [main, 'feat/**']
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npx vitest run

      - name: Checkup 深模組邊界守衛 R1-R5 (ADR-0001 / ADR-0005)
        run: npm run check:module-boundaries

      - name: ESLint — 深模組邊界 + free surface 規則 (ADR-0001 / ADR-0005)
        run: npm run lint:modules

      - name: Sitemap ↔ route consistency
        run: npx tsx scripts/sitemap-consistency-check.ts

