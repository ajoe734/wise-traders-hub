## 問題

對比三張截圖確認：持倉 tab 的 Demo 卡跟其他四個 tab（行事曆 / 事件分析 / 收盤分析 / 交易日誌 / 上傳成交）長得**完全不一樣**。

| 項目 | 持倉 tab（目前） | 其他四個 tab |
|---|---|---|
| 背景 | `alpha(C.text,'04')` 灰白 | `alpha(C.amber,'06')` 暖米色 |
| 邊框 | `C.border` 1px | `alpha(C.amber,'25')` 1px 琥珀 |
| 圓角 | 10px | 8px |
| 按鈕 | **沒有** | LINE 登入解鎖（綠）+ Email 登入（白框） |

加上下方還有第二張「登入解鎖每月 1 次免費 AI 健檢」配額卡（綠色立即登入 + 藍框查看付費方案），形成持倉頁獨有的「兩張卡 + 四種按鈕」混亂版面。

## 修改

### `src/pages/FreeCheckup.jsx`（line 3098-3103）

把持倉 tab 的提示卡改成與 events/news/daily/log 完全同款：
- 背景 `alpha(C.amber,'06')`、邊框 `alpha(C.amber,'25')`、圓角 8px
- 加入 `LINE 登入解鎖` + `Email 登入` 兩顆按鈕（onClick、色票、padding、fontSize 全部複製其他 tab 規格，確保 pixel-level 一致）
- 標題/內文字級照舊（12px 500w / 11px 1.7 line-height）

### 配額卡處理（保留）

下方「登入解鎖每月 1 次免費 AI 健檢」配額卡保留 — 它是 quota meter（顯示 used/limit 進度條給已登入用戶用），訪客 fallback 才會看到簡化版。職責不同，但因為新提示卡已含登入按鈕，配額卡可以**只在訪客模式縮小或弱化**：

選項 A（推薦）：訪客模式下隱藏配額卡，只留新的 amber 提示卡 — 視覺最乾淨，跟其他 tab 完全一致（其他 tab 也只有一張 amber 卡）。
選項 B：保留配額卡但拿掉「立即登入」按鈕（訪客已能從上方 amber 卡登入），只留「查看付費方案 →」純文字連結。

## 驗收

訪客打開 `/free-checkup`，切換 持倉 / 行事曆 / 事件分析 / 收盤分析 / 交易日誌 / 上傳成交 六個 tab，**每一頁頂部都應該看到完全相同樣式的 amber 提示卡**（同背景、同邊框、同圓角、同按鈕配置），只有標題與內文不同。

## 詢問

配額卡要走 A 還是 B？
